import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'za.co.steinrock.caplandgov3',
  appName: 'capLandGo1_v3',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
